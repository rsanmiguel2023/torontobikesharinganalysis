# Data Folder

- `raw/`: place original bike-sharing trip CSV files here.
- `processed/`: place cleaned or feature-engineered datasets here.
- `sample_us07.csv`: sample user-type data from the original project.

The Streamlit app can run without a full raw dataset because it generates sample dashboard data when no processed file is found.
